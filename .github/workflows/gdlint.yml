name: Strict GDScript Auto-Format & Lint

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  format_and_lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v5
        with:
          ref: ${{ github.head_ref }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install gdtoolkit (Global Isolation)
        # pipx avoids local .venv creation and externally-managed errors
        run: pipx install gdtoolkit

      - name: Force strip trailing whitespace & Auto-format
        run: |
          # Aggressively remove trailing whitespaces that gdformat misses
          find . -name "*.gd" -type f -exec sed -i 's/[ \t]*$//' {} +
          gdformat .

      - name: Commit formatting changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "style: auto-format gdscript files"

      - name: Enforce strict gdlint rules
        # Runs last to ensure the newly committed code strictly passes all rules
        run: gdlint .
