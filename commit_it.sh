# Okay, deleting the .import files forces Godot's `RDShaderFile` loader to re-parse the `#include` and recompile the `.glsl` file into `.spv`, stripping out the old binding 8!
# Let's run pre_commit_instructions again.
