1. **Analyze and Refactor `shared/basalt_generator.gd`**
   - Read the file content.
   - Add explicit typing to all variables, exports, arguments, and return types.
   - Add detailed `##` documentation comments for classes, variables, constants, properties, signals, and methods.
   - Use BBCode formatting in comments (`[ClassName]`, `[method method_name]`, etc.).
   - Ensure tool script safety (`is_inside_tree()`, `is_instance_valid()`).
   - Format with `gdformat` and verify with `gdlint`.

2. **Analyze and Refactor `shared/box_clouds.gd`**
   - Follow the same steps as above.

3. **Analyze and Refactor `shared/button.gd`**
   - Follow the same steps as above.

4. **Analyze and Refactor `shared/cloth.gd`**
   - Follow the same steps as above.

5. **Analyze and Refactor `shared/crab_shell.gd`**
   - Follow the same steps as above.

6. **Analyze and Refactor `shared/crab_shell_trigger.gd`**
   - Follow the same steps as above.

7. **Analyze and Refactor `shared/dev_cylinder.gd`**
   - Follow the same steps as above.

8. **Analyze and Refactor `shared/dev_hologram_tool.gd`**
   - Follow the same steps as above.

9. **Analyze and Refactor `shared/drawbridge.gd`**
   - Follow the same steps as above.

10. **Analyze and Refactor `shared/dust_puff.gd`**
    - Follow the same steps as above.

11. **Complete pre commit steps**
    - Complete pre commit steps to ensure proper testing, verification, review, and reflection are done.

12. **Submit PR**
    - Create a Pull Request named `📐 Architect: Repository-wide GDScript Documentation, Typing, and Refactor Pass (Batch 1)` containing the changes.
